#!/usr/bin/env python3
import sys
from pathlib import Path

# Add lib directory to sys.path
lib_path = Path(__file__).resolve().parent.parent / "lib"
sys.path.insert(0, str(lib_path))

from vhost_helper.main import run

if __name__ == "__main__":
    run()
